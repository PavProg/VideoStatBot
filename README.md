# Video Analytics Telegram Bot

🤖 Бот для аналитики видео с интеграцией YandexGPT и PostgreSQL. Преобразует текстовые запросы на русском языке в SQL и возвращает данные из базы.

## Содержание
1. [🚀 Быстрый старт](#быстрый-старт)
2. [🐳 Запуск через Docker (рекомендуется)](#запуск-через-docker-рекомендуется)
3. [⚙️ Ручной запуск (без Docker)](#ручной-запуск-без-docker)
4. [🔧 Настройка Telegram-бота](#настройка-telegram-бота)
5. [🏗️ Архитектура проекта](#архитектура-проекта)
6. [🧠 LLM и преобразование запросов](#llm-и-преобразование-запросов)
7. [📁 Структура проекта](#структура-проекта)
8. [📚 Документация и ссылки](#документация-и-ссылки)
9. [🤝 Вклад в проект](#вклад-в-проект)

## 🚀 Быстрый старт

Для запуска проекта локально вам понадобится:
- **Docker** и **Docker Compose** (рекомендуемый способ)
- Или **Python 3.11+**, **Poetry** и **PostgreSQL 14+** (для ручного запуска)

## 🐳 Запуск через Docker (рекомендуется)

1. **Клонируйте репозиторий**
```bash
git clone <your-repository-url>
cd video-analytics-bot
```

2. **Настройте переменные окружения**
Создайте файл `.env` в корне проекта на основе примера:
```bash
cp .env.example .env
```
Отредактируйте `.env` файл, задав все необходимые переменные (см. раздел [Настройка Telegram-бота](#настройка-telegram-бота)).

3. **Запустите сервисы**
```bash
docker-compose up -d
```

4. **Загрузите начальные данные в БД** (опционально)
```bash
docker-compose exec app python src/services/data_loader/loader_service.py
```

5. **Проверьте работоспособность**
```bash
docker-compose logs -f app
```

Контейнеры будут запущены:
- **app** – Telegram бот на Python
- **postgres** – База данных PostgreSQL
- **pgadmin** – Веб-интерфейс для управления БД (опционально, доступен на `localhost:5050`)

## ⚙️ Ручной запуск (без Docker)

### Предварительные требования
- **Python 3.11+**
- **Poetry** (менеджер зависимостей)
- **PostgreSQL 14+**

### Установка

1. **Клонируйте и настройте окружение**
```bash
git clone <your-repository-url>
cd video-analytics-bot
```

2. **Установите зависимости через Poetry**
```bash
poetry install
poetry shell  # Активируйте виртуальное окружение
```

3. **Настройте базу данных PostgreSQL**
```bash
# Создайте базу данных
createdb video_analytics

# Или выполните в psql:
# CREATE DATABASE video_analytics;
```

4. **Настройте переменные окружения**
Создайте файл `.env` в корне проекта:
```env
# Обязательные настройки
TOKEN=your_telegram_bot_token_here
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=your_password
DB_NAME=video_analytics

# Настройки Yandex Cloud
YC_API_KEY=your_yandex_cloud_api_key
YC_FOLDER_ID=your_yandex_folder_id
YC_MODELS=yandexgpt-lite
YC_TEMPERATURE=0.5
YC_MAX_TOKENS=1000

# Настройки логирования
LOG_LEVEL=INFO
LOG_FILE=logs/app.log
```

5. **Инициализируйте базу данных и загрузите данные**
```bash
# Создание таблиц
python src/db/database.py

# Загрузка данных из JSON
python src/services/data_loader/loader_service.py
```

6. **Запустите бота**
```bash
python src/main.py
```

## 🔧 Настройка Telegram-бота

### 1. Создание бота через @BotFather
1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Укажите имя бота (например, `Video Analytics Bot`)
   - Укажите username бота (должен заканчиваться на `bot`, например `video_analytics_bot`)
4. Скопируйте полученный **токен** (выглядит как `1234567890:ABCdefGHIjklmNOPqrstUVwxyz`)

### 2. Настройка токена в проекте
Добавьте токен в файл `.env`:
```env
TOKEN=1234567890:ABCdefGHIjklmNOPqrstUVwxyz
```

### 3. Проверка работы бота
1. Найдите бота в Telegram по username (например, `@video_analytics_bot`)
2. Начните диалог командой `/start`
3. Отправьте текстовый запрос на русском языке

**Примеры запросов:**
- "Сколько всего видео в базе?"
- "Сколько видео получали новые просмотры 27 ноября 2025?"
- "Какой автор имеет больше всего просмотров?"

## 🏗️ Архитектура проекта

Проект построен по модульному принципу с четким разделением ответственности:

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Telegram Bot  │◄──►│     LLM Layer   │◄──►│  Database Layer │
│   (Aiogram)     │    │  (YandexGPT)    │    │   (SQLAlchemy)  │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Handlers      │    │  Prompt Engine  │    │   Models & ORM  │
│   & Routing     │    │  & Validation   │    │   & Sessions    │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

### Ключевые компоненты:
- **Telegram Bot Layer** – обработка входящих сообщений и команд
- **LLM Integration Layer** – преобразование текста в SQL через YandexGPT
- **Database Abstraction Layer** – работа с PostgreSQL через асинхронный SQLAlchemy
- **Data Loader** – загрузка и обработка начальных данных из JSON

## 🧠 LLM и преобразование запросов

### Подход к преобразованию текста в SQL

Система использует **промпт-инженерию** для безопасного преобразования естественного языка в SQL:

1. **Контекстуализация запроса**: Пользовательский запрос дополняется описанием схемы БД
2. **Генерация SQL**: YandexGPT создает SQL-запрос на основе контекста
3. **Валидация безопасности**: Проверка запроса на отсутствие опасных операций
4. **Выполнение в БД**: Запрос выполняется и результат возвращается пользователю

### Пример промпта для LLM:

```python
system_prompt = """
Ты — помощник для работы с базой данных статистики видео.
СХЕМА БАЗЫ ДАННЫХ:
1. Таблица 'videos' (видео): видео_id, creator_id, views_count, ...
2. Таблица 'snapshots' (снапшоты): snapshot_id, video_id, delta_views_count, ...

ВАЖНЫЕ ПРАВИЛА:
1. Запрос должен возвращать ОДНО ЧИСЛО
2. Используй только SELECT запросы
3. Для вопросов о датах используй DATE(): WHERE DATE(created_at) = '2025-11-27'
4. Для вопросов о "новых просмотрах" используй delta_views_count > 0
"""
```

### Запуск main.py

Основной файл `src/main.py` инициализирует и запускает бота:

```python
import asyncio
import logging
from aiogram import Bot, Dispatcher
from src.config.config import settings
from src.handlers import router

async def main():
    bot = Bot(token=settings.RE_TOKEN)
    dp = Dispatcher()
    dp.include_router(router)
    
    await dp.start_polling(bot)

if __name__ == "__main__":
    logging.basicConfig(level=logging.INFO)
    asyncio.run(main())
```

## 📁 Структура проекта

```
video-analytics-bot/
├── src/
│   ├── config/               # Конфигурация приложения
│   │   └── config.py         # Настройки Pydantic
│   ├── db/                   # Работа с базой данных
│   │   ├── database.py       # Подключение и сессии
│   │   └── models.py         # SQLAlchemy модели
│   ├── llm_service/          # Интеграция с LLM
│   │   └── llm_service.py    # Сервис работы с YandexGPT
│   ├── services/             # Бизнес-логика
│   │   └── data_loader/      # Загрузчик данных
│   ├── handlers/             # Обработчики Telegram
│   │   └── handlers.py       # Хендлеры сообщений
│   └── main.py               # Точка входа
├── data/                     # Данные для загрузки
│   └── videos.json           # Пример данных
├── logs/                     # Логи приложения
├── .env.example              # Шаблон переменных окружения
├── .env                      # Файл с переменными окружения
├── docker-compose.yml        # Docker Compose конфигурация
├── Dockerfile                # Docker образ приложения
├── pyproject.toml            # Зависимости Poetry
└── README.md                 # Документация
```

## 📚 Документация и ссылки

### Основные технологии

| Технология | Назначение | Документация |
|------------|------------|--------------|
| **Aiogram 3.x** | Асинхронный фреймворк для Telegram ботов | [Документация Aiogram](https://docs.aiogram.dev/) |
| **SQLAlchemy 2.0** | Асинхронный ORM для работы с PostgreSQL | [Документация SQLAlchemy](https://docs.sqlalchemy.org/) |
| **Yandex Cloud ML SDK** | Интеграция с YandexGPT API | [GitHub репозиторий](https://github.com/yandex-cloud/cloudapi) |
| **Pydantic Settings** | Управление конфигурацией приложения | [Документация Pydantic](https://docs.pydantic.dev/) |

### Yandex Cloud и LLM

1. **Yandex Cloud Documentation**
   - [Обзор YandexGPT](https://cloud.yandex.ru/services/yandexgpt)
   - [Начало работы с Yandex Cloud](https://cloud.yandex.ru/docs/overview/)
   - [Создание API-ключа](https://cloud.yandex.ru/docs/iam/operations/api-key/create)

2. **YandexGPT API**
   - [REST API документация](https://cloud.yandex.ru/docs/yandexgpt/api-ref/)
   - [Примеры использования](https://cloud.yandex.ru/docs/yandexgpt/concepts/prompt)
   - [Настройки моделей](https://cloud.yandex.ru/docs/yandexgpt/concepts/models)

3. **Python SDK для Yandex Cloud**
   - [yandex-cloud-ml-sdk](https://pypi.org/project/yandex-cloud-ml-sdk/)
   - [Примеры кода](https://github.com/yandex-cloud/cloudapi/tree/master/python)

---

*Примечание: Для работы с Yandex Cloud требуется активный аккаунт и настроенный сервисный аккаунт с соответствующими правами доступа.*
